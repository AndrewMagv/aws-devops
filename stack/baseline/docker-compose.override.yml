ambassador:
    command:
        - "--addr"
        - "0.0.0.0:29091"
        - "--advertise"
        - "${HostIP}:2375"
        - "--proxy"
        - '{"net": "tcp", "src": ":2379", "dst": ["${DiscoveryHost}:2379"]}'
        - "etcd://${DiscoveryHost}:2379"
    external_links:
        - "redis"
